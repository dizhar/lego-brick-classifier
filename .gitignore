# === Python & Virtual Environments ===
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python
env/
venv/
.venv/
ENV/
env.bak/
venv.bak/
pip-log.txt
pip-delete-this-directory.txt
.pip-cache/

# === Streamlit ===
.streamlit/
.streamlit/secrets.toml
secrets.toml

# === Jupyter ===
.ipynb_checkpoints

# === IDEs & Editors ===
.idea/
.vscode/
*.swp
*.swo
*~
*.iml

# === OS Files ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# === Local Environment ===
.env
.env.local
.env.*.local
*.log

# === Build & Distribution ===
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# === Testing ===
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.pytest_cache/

# === ML-Specific: Ignore large/raw data & checkpoints ===
data/raw/
data/processed/
data/interim/
data/external/
*.zip
*.tar.gz
*.csv
*.json
*.parquet
*.feather
checkpoints/
logs/
tensorboard/
wandb/
mlflow/

# === DO NOT IGNORE: Your actual project assets ===
# We WANT to track:
# - data/*.csv, *.json → small datasets or configs
!data/
!data/**/*
!models/

# === Model weights: pull from GCS, never commit ===
# Large binary weights bloat git history forever. They live in GCS
# (gs://briqvision-training) and are provisioned with:
#   ./scripts/download_models.sh
models/**/*.pt
models/**/*.pth
models/**/*.onnx
models/**/*.h5
models/**/*.tflite
models/.DS_Store

# === Optional: Use Git LFS for large model/image files ===
# (Uncomment if you install Git LFS)
# *.h5 filter=lfs diff=lfs merge=lfs -text
# *.pth filter=lfs diff=lfs merge=lfs -text
# *.pt filter=lfs diff=lfs merge=lfs -text
# *.pkl filter=lfs diff=lfs merge=lfs -text
# *.onnx filter=lfs diff=lfs merge=lfs -text
# *.tflite filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text

# === Custom Local Paths ===
local/
temp/
tmp/.env
